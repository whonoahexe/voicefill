---
phase: 03-package-ship
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - assets/js/parser.js
  - assets/js/ui.js
autonomous: true
requirements:
  - PARSE-05

must_haves:
  truths:
    - "User drops an Instagram export ZIP and receives a reconstructed chat log with voice messages transcribed (or annotated [Instagram voice: media expired] for CDN-only URIs)"
    - "Instagram messages appear oldest-first — not newest-first as exported"
    - "Non-ASCII sender names and message content are readable — no garbled Latin-1 sequences"
    - "Dropping a non-Instagram ZIP continues to route to the existing WhatsApp parser without error"
    - "A standalone Instagram JSON file (without ZIP) is detected and routes to the text-only chat log (no audio)"
  artifacts:
    - path: "assets/js/parser.js"
      provides: "parseInstagram(file) export following same return shape as parseZip()"
      exports: ["parseInstagram"]
    - path: "assets/js/ui.js"
      provides: "Instagram drop/pick routing, exportMode:'instagram' branch in processFile"
      contains: "parseInstagram"
  key_links:
    - from: "assets/js/ui.js (drop handler)"
      to: "assets/js/parser.js parseInstagram()"
      via: "file.name.endsWith('.zip') && isInstagramZip() → parseInstagram(file)"
      pattern: "parseInstagram"
    - from: "assets/js/parser.js parseInstagram()"
      to: "audioFiles Map (basename → ZipObject)"
      via: "uri.split('/').pop() basename extraction (Pitfall 5 guard)"
      pattern: "split.*pop"
---

<objective>
Add Instagram JSON export parsing to the existing WhatsApp parser, wire the Instagram route into the UI drop/pick handlers, and apply the Latin-1 encoding fix to all Instagram string fields. The Whisper transcription queue from Phase 2 works unchanged — .m4a audio from Instagram passes through the same audio decode and RMS silence gate as .opus from WhatsApp.

Purpose: Deliver PARSE-05. The app accepts a second input format without breaking the existing WhatsApp path.

Output: `parseInstagram()` in parser.js + Instagram routing in ui.js. No new screens required — results reuse the existing renderChatLog() + dispatchTranscription() pipeline.
</objective>

<execution_context>
@G:/Users/whono/.claude/get-shit-done/workflows/execute-plan.md
@G:/Users/whono/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-package-ship/03-CONTEXT.md
@.planning/phases/03-package-ship/03-RESEARCH.md
@.planning/phases/03-package-ship/03-01-SUMMARY.md

<interfaces>
<!-- Key contracts the executor must implement against. Extracted from codebase. -->

From assets/js/parser.js — parseZip() return shape (parseInstagram must return same shape):
  {
    mode: 'with-media' | 'without-media',   // always 'with-media' for Instagram ZIPs with audio
    exportMode: 'whatsapp' | 'instagram',   // NEW: 'instagram' for parseInstagram()
    messages: Array<MessageObject>,          // see MessageObject shape below
    audioFiles: Map<string, ZipObject>,      // basename → JSZip entry (bytes read lazily)
    plainText: string,                       // assemblePlainText(messages) output
    stats: { voiceTotal: number, voiceMatched: number }
  }

MessageObject shape (from parseChatText / matchVoiceToAudio):
  {
    type: 'text' | 'voice' | 'voice-omitted' | 'system',
    timestamp: string,       // display string, e.g. "5/21/24, 9:00 AM"
    sender: string,          // sender display name
    content: string,         // message text body (empty for voice)
    basename?: string,       // audio filename for type:'voice' messages
    matched?: boolean,       // true if audioFiles Map has this basename
    annotation?: string,     // e.g. '[Audio file missing]'
    audioEntry?: ZipObject   // JSZip entry ref when matched:true
  }

From assets/js/ui.js — processFile() pattern (reference for new processInstagram integration):
  async function processFile(file) {
    showScreen('processing');
    try { result = await parseZip(file); } catch (err) { showParseError(err.message); return; }
    if (result.mode === 'without-media') { showScreen('without-media'); }
    else { renderChatLog(result); showScreen('results'); await dispatchTranscription(result); }
  }

From assets/js/ui.js — init() drop handler (current, to be extended):
  dropZone.addEventListener('drop', (e) => {
    const file = e.dataTransfer.files[0];
    if (!file) return;
    if (!file.name.toLowerCase().endsWith('.zip')) return;  // silently ignores non-ZIP
    processFile(file);
  });

From assets/js/ui.js — fileInput change handler (current, to be extended):
  fileInput.addEventListener('change', (e) => {
    const file = e.target.files[0];
    e.target.value = '';
    processFile(file);  // routes all ZIP files here
  });

From assets/js/ui.js — dispatchTranscription() (Phase 2, unchanged):
  // Reads result.messages, queues voice rows where msg.matched === true
  // Handles both File (folder mode) and ZipObject (ZIP mode) via getAudioBytes()
  // Instagram .m4a ZipObjects pass through the same decode path as WhatsApp .opus
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add parseInstagram() to parser.js</name>
  <files>assets/js/parser.js</files>
  <read_first>
    - assets/js/parser.js — read parseZip() in full (lines ~207–280) to understand the return shape, audioFiles Map construction, and 500MB size guard pattern to replicate
    - assets/js/parser.js — read assemblePlainText() (lines ~172–195) — reuse for plainText generation
    - .planning/phases/03-package-ship/03-RESEARCH.md — Pattern 4 (Instagram parser structure), Instagram Format Research section, Pitfalls 4, 5, 6
  </read_first>
  <behavior>
    - parseInstagram(non-zip-file) throws 'Instagram export must be a .zip file'
    - parseInstagram(zip-over-500MB) throws 'ZIP file exceeds 500MB limit'
    - parseInstagram(zip-with-no-message-json) throws 'No Instagram message file found in ZIP'
    - parseInstagram(zip-with-invalid-json) throws 'Unrecognized format -- not an Instagram message export'
    - parseInstagram(valid-zip) returns object with exportMode:'instagram', messages sorted oldest-first by timestamp_ms
    - voice message with audio_files[0].uri = 'path/to/audioclip_123.m4a' matches audioFiles.get('audioclip_123.m4a')
    - voice message with voice_media = 'https://...' produces matched:false, annotation:'[Instagram voice: media expired — download the export again]'
    - sender_name with garbled Latin-1 bytes is fixed by fixInstagramEncoding() before display
    - messages with no audio_files and no voice_media produce type:'text' entries
    - return stats.voiceTotal counts all voice-type messages; stats.voiceMatched counts matched:true entries
  </behavior>
  <action>
    Add a `fixInstagramEncoding(str)` helper function (not exported) before the `parseInstagram` export. This helper re-encodes garbled Latin-1 bytes back to UTF-8 using the `escape` / `decodeURIComponent` approach:
      try { return decodeURIComponent(escape(str)); } catch { return str; }
    Apply `fixInstagramEncoding()` to every string field read from Instagram JSON before storing it in a message object: `sender_name`, `content`, and any `title` field.

    Add an `isVoiceMessage(msg)` helper (not exported):
      returns true if `Array.isArray(msg.audio_files) && msg.audio_files.length > 0`
      OR `typeof msg.voice_media === 'string' && msg.voice_media !== ''`

    Add a `getInstagramAudioBasename(msg)` helper (not exported):
      If audio_files present: return `msg.audio_files[0].uri.split('/').pop()` (Pitfall 5 guard)
      Otherwise: return null

    Add `export async function parseInstagram(file)` following RESEARCH.md Pattern 4:
    1. Validate file extension ends with '.zip'; throw 'Instagram export must be a .zip file' otherwise
    2. Apply the 500MB size guard (same threshold as parseZip: file.size > 500 * 1024 * 1024)
    3. Use `JSZip.loadAsync(file)` to load the ZIP (same global window.JSZip as parseZip)
    4. Iterate zip entries with `zip.forEach`: collect `message_N.json` files matched by regex `/^message_\d+\.json$/` tested against `basename` (last path segment); collect `.m4a` files into `audioFiles` Map keyed by basename
    5. If no message JSON found, throw 'No Instagram message file found in ZIP'
    6. For each message JSON entry: parse JSON string, validate `Array.isArray(raw.participants) && Array.isArray(raw.messages)`, throw 'Unrecognized format -- not an Instagram message export' on invalid
    7. Merge all `raw.messages` arrays from all message JSON files; sort ascending by `timestamp_ms`
    8. Map each Instagram message object to the MessageObject shape:
       - `type`: if isVoiceMessage(msg) → 'voice', else 'text'
       - `timestamp`: new Date(msg.timestamp_ms).toLocaleString() for display
       - `sender`: fixInstagramEncoding(msg.sender_name)
       - `content`: fixInstagramEncoding(msg.content || '')
       - For voice type: `basename` = getInstagramAudioBasename(msg); if basename and audioFiles.has(basename) → `matched:true`, `audioEntry` = audioFiles.get(basename); else if audio_files present but no match → `matched:false`, `annotation:'[Audio file missing]'`; if voice_media is an https:// URL → `matched:false`, `annotation:'[Instagram voice: media expired — download the export again]'`
    9. Compute stats: `voiceTotal` = messages.filter(m => m.type === 'voice').length; `voiceMatched` = messages.filter(m => m.type === 'voice' && m.matched).length
    10. Return `{ mode: 'with-media', exportMode: 'instagram', messages, audioFiles, plainText: assemblePlainText(messages), stats }`

    The `assemblePlainText` function already handles type:'voice' with matched:true/false correctly — no changes needed to that function.
  </action>
  <verify>
    <automated>node --input-type=module --eval "
import { parseInstagram } from './assets/js/parser.js';
// Verify export exists and is a function
if (typeof parseInstagram !== 'function') throw new Error('parseInstagram not exported');
console.log('parseInstagram export OK');
" 2>&1 || echo "SKIP: ESM test requires full browser context — verify via grep instead"</automated>
    <automated>node -e "const fs = require('fs'); const src = fs.readFileSync('assets/js/parser.js','utf8'); console.assert(src.includes('export async function parseInstagram'), 'parseInstagram not exported'); console.assert(src.includes('fixInstagramEncoding'), 'fixInstagramEncoding missing'); console.assert(src.includes(\"split('/').pop()\"), 'basename extraction missing (Pitfall 5)'); console.assert(src.includes('timestamp_ms'), 'timestamp_ms sort missing'); console.assert(src.includes('media expired'), 'voice_media fallback annotation missing'); console.log('parser.js assertions OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `export async function parseInstagram(file)` present in assets/js/parser.js
    - `fixInstagramEncoding(str)` helper present using `decodeURIComponent(escape(str))` pattern
    - `uri.split('/').pop()` used for audio basename extraction (Pitfall 5 guard)
    - Messages sorted by `timestamp_ms` ascending before mapping (Pitfall 6 guard)
    - `voice_media` string presence produces `matched:false` with annotation `'[Instagram voice: media expired — download the export again]'`
    - Return object contains `exportMode: 'instagram'` and `mode: 'with-media'`
    - All Instagram string fields passed through `fixInstagramEncoding()` before use
    - `assemblePlainText` is reused (not duplicated) for the `plainText` field
    - 500MB size guard present (same pattern as parseZip)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire Instagram route in ui.js</name>
  <files>assets/js/ui.js</files>
  <read_first>
    - assets/js/ui.js — read init() in full (lines ~525–end) to understand all drop and file picker event handlers that must be extended
    - assets/js/ui.js — read processFile() (lines ~432–459) to understand the routing pattern to replicate for Instagram ZIPs
    - assets/js/ui.js — read dispatchTranscription() (lines ~379–424) — confirm it works with any result object that has messages+audioFiles (it does; no changes needed)
    - .planning/phases/03-package-ship/03-RESEARCH.md — Instagram detection heuristic (isInstagramJSON function) and D-07 decision
  </read_first>
  <action>
    Update the import line at the top of ui.js to add `parseInstagram` to the parser.js import:
      `import { parseZip, parseFolder, parseTxt, parseInstagram } from './parser.js';`

    Add a `isInstagramZip` detection strategy: after a ZIP is loaded into JSZip (inside a new helper or inline in processFile), check the first message_N.json found; if it has `participants` + `messages` + `thread_path` at root level, it's an Instagram export. However, to avoid loading the ZIP twice, use a lighter heuristic in the drop handler: if the ZIP filename matches `/instagram.*\.zip$/i`, try parseInstagram first. If that throws 'Unrecognized format', fall back to parseZip. For all other ZIPs: go straight to parseZip. This avoids any JSZip double-load for WhatsApp files.

    More concretely — create a new `async function processZipFile(file)` (replaces the direct `processFile` call for ZIP routes):
    1. `showScreen('processing')`; record `start = Date.now()`
    2. Try `parseInstagram(file)` first IF `file.name.toLowerCase().includes('instagram')` — if successful (no throw), proceed as Instagram result
    3. Otherwise (non-instagram filename OR parseInstagram threw 'Unrecognized format'), try `parseZip(file)`
    4. On any error from both: `showScreen('upload'); showParseError(err.message); return`
    5. Enforce 300ms minimum display: `const elapsed = Date.now() - start; if (elapsed < 300) await new Promise(r => setTimeout(r, 300 - elapsed))`
    6. Route result: if `result.mode === 'without-media'` → `showScreen('without-media')`; else → `renderChatLog(result); showScreen('results'); await dispatchTranscription(result)`

    Also handle `.zip` files that are Instagram but don't have "instagram" in the filename: in the else branch of step 2, after parseZip succeeds, if the result has no `exportMode` set it is WhatsApp — fine. If parseZip throws, do NOT try parseInstagram again as a second fallback (avoid confusing errors).

    Update all three ZIP-accepting event handlers in init() to call `processZipFile(file)` instead of `processFile(file)`:
    - The drop zone `drop` event handler
    - The `fileInput` change handler (btn-browse picks ZIP files)

    Also extend the drop zone to accept `.json` files for Instagram text-only mode (D-07):
    - In the drop handler, add a branch: if `file.name.toLowerCase().endsWith('.json')` → call a new `processInstagramJson(file)` async function
    - `processInstagramJson(file)`: loads file as text via `file.text()`, parses JSON, checks `isInstagramJSON(parsed)` heuristic (participants + messages + thread_path), if valid → maps messages to MessageObject shape (text only, no audio, voiceMatched:0), calls `renderChatLog(result); showScreen('results')` — does NOT call dispatchTranscription (no audio files). If JSON is not Instagram format: `showParseError('Unrecognized file — drop a WhatsApp or Instagram export ZIP')`

    Add the `isInstagramJSON(parsed)` helper function (not exported) to ui.js:
      `Array.isArray(parsed.participants) && Array.isArray(parsed.messages) && typeof parsed.thread_path === 'string'`

    The existing `file-input` accept attribute in index.html is `.zip` — leave it. The `.json` input path is drop-only (no browse button needed for this format in v1).

    XSS rule: all Instagram sender names and message content set via textContent — renderMessage() already enforces this; no changes to renderMessage() needed.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const src = fs.readFileSync('assets/js/ui.js','utf8'); console.assert(src.includes('parseInstagram'), 'parseInstagram import missing'); console.assert(src.includes('processZipFile'), 'processZipFile missing'); console.assert(src.includes('isInstagramJSON'), 'isInstagramJSON helper missing'); console.assert(src.includes('processInstagramJson'), 'processInstagramJson missing'); console.log('ui.js assertions OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `parseInstagram` added to the import statement from './parser.js'
    - `async function processZipFile(file)` present, called from drop and fileInput change handlers instead of processFile
    - Instagram filename heuristic (`file.name.toLowerCase().includes('instagram')`) used to route to parseInstagram first
    - `isInstagramJSON(parsed)` helper present in ui.js checking `participants`, `messages`, `thread_path`
    - `async function processInstagramJson(file)` present for drop-only JSON handling
    - Drop handler accepts `.json` files in addition to `.zip` files
    - `dispatchTranscription()` is NOT called for JSON-only (text-only) Instagram imports — no audio path
    - All existing WhatsApp routing (parseZip, parseFolder, parseTxt handlers) is unchanged
    - No `.innerHTML` assignments added anywhere in ui.js
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User filesystem → parser.js | Instagram ZIP bytes are untrusted; JSON fields are untrusted strings |
| Instagram JSON fields → DOM | sender_name, content must never reach innerHTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering | Instagram sender_name / content → DOM | mitigate | textContent enforced via existing renderMessage(); fixInstagramEncoding() is display-only, does not eval content |
| T-03-06 | Tampering | audio_files[].uri path traversal | mitigate | Only basename used (uri.split('/').pop()); never resolved as filesystem path; Map lookup only |
| T-03-07 | Denial of Service | Instagram ZIP bomb | mitigate | 500MB size guard in parseInstagram() mirrors parseZip() guard |
| T-03-08 | Tampering | Instagram JSON injection via voice_media https:// URL | mitigate | voice_media URLs produce static annotation string '[Instagram voice: media expired...]' via textContent; URL is never fetched or rendered as HTML |
| T-03-SC | Tampering | npm packages | accept | No new packages in 03-02; Instagram parser is pure JS using existing JSZip global |
</threat_model>

<verification>
- Drop an Instagram export ZIP → chat log renders oldest-first with voice rows showing [Voice message: transcription pending] that update when Whisper finishes
- Drop a WhatsApp ZIP with the same app session → WhatsApp route still works correctly (regression check)
- Drop a non-Instagram non-WhatsApp ZIP → showParseError fires with an appropriate message
- Drop a standalone Instagram .json file → text-only chat log renders without audio; no Whisper jobs dispatched
- Sender name with accented characters (e.g. "José") renders correctly — not garbled
- parser.js exports grep: `grep -c "export async function parseInstagram" assets/js/parser.js` returns 1
- ui.js grep: `grep -c "parseInstagram" assets/js/ui.js` returns at least 2 (import + usage)
</verification>

<success_criteria>
- PARSE-05: Instagram export ZIP processed → reconstructed chat log with voice messages transcribed (or annotated with graceful fallback)
- Instagram messages appear in chronological order (oldest first) — Pitfall 6 guard confirmed
- Non-ASCII names render correctly — Latin-1 encoding fix confirmed
- Audio basename extraction works — uri full-path stripped to filename only (Pitfall 5 guard confirmed)
- WhatsApp path regression-free — existing WhatsApp ZIP processing unchanged
</success_criteria>

<output>
Create `.planning/phases/03-package-ship/03-02-SUMMARY.md` when done.
</output>
